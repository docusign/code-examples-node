<%- include("../../partials/examplesHead") %>

<%- include("../../partials/exampleInfo") %>

<form action="" class="eg" method="post" data-busy="form">
    <div>
        <% if(example.Forms && example.Forms[0].FormName) { %>
          <%- example.Forms[0].FormName %>
        <% } %>
        
        <div class="form-group">
            <label for="signer1_email"><%= example.Forms[0].Inputs[0].InputName %></label>
            <input type="email" class="form-control" id="signer1Email" name="signer1Email"
                   aria-describedby="emailHelp" placeholder="<%= example.Forms[0].Inputs[0].InputPlaceholder %>" required
                   value="<%= locals.dsConfig.signerEmail %>" />
            <small id="emailHelp" class="form-text text-muted"><%= locals.manifest.SupportingTexts.HelpingTexts.EmailWontBeShared %></small>
        </div>
        <div class="form-group">
            <label for="signer1_name"><%= example.Forms[0].Inputs[1].InputName %></label>
            <input type="text" class="form-control" id="signer1Name" placeholder="<%= example.Forms[0].Inputs[1].InputPlaceholder %>" name="signer1Name"
                   value="<%= locals.dsConfig.signerName %>" required />
        </div>
    </div>
    <div>
        <% if(example.Forms && example.Forms[0].FormName) { %>
          <%- example.Forms[0].FormName %>
        <% } %>
        
        <div class="form-group">
            <label for="signer2aEmail"><%= example.Forms[1].Inputs[0].InputName %></label>
            <input type="email" class="form-control" id="signer2aEmail" name="signer2aEmail"
                   aria-describedby="emailHelp" placeholder="<%= example.Forms[1].Inputs[0].InputPlaceholder %>" required
                   value="" />
            <small id="emailHelp" class="form-text text-muted">
                The email for the second recipient must be different from the signer's email.
            </small>
        </div>
        <div class="form-group">
            <label for="signer2aName"><%= example.Forms[1].Inputs[1].InputName %></label>
            <input type="text" class="form-control" id="signer2aName" placeholder="<%= example.Forms[1].Inputs[1].InputPlaceholder %>" name="signer2aName"
                   value="" required />
            <br/>
        </div>
    </div>
    <div>
        <% if(example.Forms && example.Forms[0].FormName) { %>
          <%- example.Forms[0].FormName %>
        <% } %>
        
        <div class="form-group">
            <label for="signer2bEmail"><%= example.Forms[2].Inputs[0].InputName %></label>
            <input type="email" class="form-control" id="signer2bEmail" name="signer2bEmail"
                   aria-describedby="emailHelp" placeholder="<%= example.Forms[2].Inputs[0].InputPlaceholder %>" required
                   value="" />
            <small id="emailHelp" class="form-text text-muted">
                The email for the second recipient must be different from the signer's email.
            </small>
        </div>
        <div class="form-group">
            <label for="signer2bName"><%= example.Forms[2].Inputs[1].InputName %></label>
            <input type="text" class="form-control" id="signer2bName" placeholder="<%= example.Forms[2].Inputs[1].InputPlaceholder %>" name="signer2bName"
                   value="" required />
            <br/>
        </div>
    </div>
    <input type="hidden" name="_csrf" value="<%- csrfToken %>">
    <%- include("../../partials/submitButton") %>
</form>

<%- include("../../partials/examplesFoot") %>
